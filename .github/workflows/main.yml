name: Build Binaries

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Enter the version (e.g., v1.0.0)"
        required: true

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build executable
        run: pyinstaller main.py --add-data assets:assets --add-data levels:levels

      - name: List files
        run: ls -R

      - name: Rename executable
        run: |
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            mv dist/main/main.exe dist/mazetest2-win.exe
          elif [[ "$RUNNER_OS" == "Linux" ]]; then
            mv dist/main dist/mazetest2-linux
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            mv dist/main dist/mazetest2-mac
          fi
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mazetest2-${{ matrix.os }}
          path: dist/*


  download:
    if: ${{ always() }}
    needs: build
    runs-on: ubuntu-latest
    steps:

      - name: List downloaded files (Debugging)
        run: ls -R

      - name: Make files executable (Linux/Mac)
        run: chmod +x mazetest2-* || true

      - name: Pack game files
        run: |
          mkdir mazetest2
          mv main/_internal/assets mazetest2/assets
          mv main/_internal/levels mazetest2/levels
          zip -r mazetest2-files.zip mazetest2/
          zip -u mazetest2-files.zip mazetest2-linux
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: mazetest2-files.zip
          path: dist/*

      - name: Download all build artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: MazeTest 2 ${{ github.event.inputs.version }}
          draft: true
          prerelease: false
          files: |
            ./mazetest2-linux
            ./mazetest2-win.exe
            ./mazetest2-mac
            ./mazetest2-files.zip
